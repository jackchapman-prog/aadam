# AADAM

Amigurumi pattern generator (static site). Open `index.html` locally, or use the hosted link on GitHub Pages.

## Live link

After the first deploy: `https://jackchapman-prog.github.io/aadam/`

## Local

Open `index.html` in a browser, or:

```bash
npx --yes serve .
```
